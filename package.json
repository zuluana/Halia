{
  "name": "halia",
  "version": "0.1.0",
  "description": "Runtime Module Management - Build 'Feature APIs' with simple dependency injection",
  "author": "Oranda",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "build": "npm run build-ts && npm run tslint",
    "test": "mocha ./dist/test/**/*.test.js",
    "build-ts": "tsc --declaration",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "prepare": "npm run build",
    "tslint-fix": "tslint -c tslint.json -p tsconfig.json --fix"
  },
  "devDependencies": {
    "@types/expect": "^1.20.3",
    "@types/mocha": "^5.2.5",
    "@types/node": "^9.6.55",
    "chai": "^4.2.0",
    "expect": "^24.1.0",
    "mocha": "^5.2.0",
    "ts-node": "^5.0.0",
    "tslint": "^5.12.1",
    "typescript": "^3.5.3"
  }
}
